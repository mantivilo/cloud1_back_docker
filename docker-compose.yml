version: '1'
services:
  microservicio:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./target/micro-pacientes-0.0.1-SNAPSHOT.jar:/app/app.jar
    ports:
      - "8082:8082"
    environment:
      - spring.datasource.url=jdbc:oracle:thin:@ctagtsp6ga68lfba_high?TNS_ADMIN=/app/oracle_wallet/
      - spring.datasource.username=ADMIN
      - spring.datasource.password=Bayonetta2024
    restart: always